{
  "name": "qanoontech-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Control plane for a QanoonTech deployment",
  "license": "UNLICENSED",
  "type": "module",
  "engines": { "node": ">=22" },
  "bin": { "qanoontech-engine": "./dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "engine": "tsx src/cli.ts",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "yaml": "^2.7.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
